;; Add to the language of exercise 4.29 a procedure arraylength, which
;; returns the size of an array. Your procedure should work in
;; constant time. Make sure that arrayref and arrayset check to make
;; sure that their indices are within the length of the array.

;; see eopl.core.mutable-pair-lang
