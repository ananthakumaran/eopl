;; We can get some of the benefits of call-by-reference without
;; leaving the call-by-value framework. Extend the language
;; implicit-refs by adding a new expression

;; Expression ::= ref Identifier
;;                ref-exp (var)


;; see eopl.core.implicit-ref-lang
