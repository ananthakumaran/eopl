;; Extend the check to handle multiple let declarations, multiargument
;; procedures, and multiple letrec declarations. You will need to add
;; types of the for (t1 * t2 * ... * tn -> t) to handle multiargument
;; procedures.

;; see eopl.core.checker-lang
