;; Add a list expression to the language, as in exercise 3.10. As a
;; hint, consider adding two new continuation-builders, one for
;; evaluating the first element of the list and one for evaluating the
;; rest of the list.

;; see eopl.core.letrec-cont-lang
