;; As an alternative to the preceding exercise, add a new nonterminal
;; Bool-exp of boolean expressions to the language. Change the
;; production for conditional expressions to say

;; Expression ::= if Bool-exp then Expression else Expression

;; Write suitable productions for Bool-exp and implement value-of-bool-exp.
;; Where do the predicates of ex 3.8 wind up in this organization

;; see core/let_lang.clj

